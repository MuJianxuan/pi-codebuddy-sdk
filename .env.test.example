# Maintainer-only: copy to .env.test (gitignored) for cross-provider integration tests.
# End users do not need this file.
CODEBUDDY_SDK_TESTING_ALT_PROVIDER=openai
CODEBUDDY_SDK_TESTING_ALT_MODEL=gpt-4o-mini
